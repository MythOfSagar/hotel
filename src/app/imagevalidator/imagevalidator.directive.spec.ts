import { ImagevalidatorDirective } from './imagevalidator.directive';

describe('ImagevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagevalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
